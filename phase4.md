# Phase 4 — 10번째 프레임(마지막 프레임) 및 퍼펙트 게임

## 배경
이 리포지토리는 Visual Studio C++ 프로젝트이며, gmock을 사용해 테스트를 작성한다. 빌드/테스트는 리포지토리 루트의 `build-and-test.ps1`로 수행한다 (MSBuild로 `bowling-tdd.slnx`를 빌드한 뒤 `x64\Debug\bowling-tdd.exe`를 실행, 종료 코드로 성공/실패 판정).

이전 phase들에서 이미 다음이 갖춰져 있다고 가정한다:
- gmock 테스트 실행 진입점과 `bowling-tdd.exe`가 gmock 테스트 러너로 동작하도록 프로젝트 설정이 완료됨.
- `Game` 클래스(`roll(int pins)`, `score()`)가 구현되어 있고, 다음이 이미 정확히 계산됨:
  - 오픈 프레임 (두 투구 합 10 미만).
  - 스페어 프레임 (두 투구 합 정확히 10, 보너스 = 다음 투구 1회).
  - 스트라이크 프레임 (첫 투구 10핀, 보너스 = 다음 두 투구의 합), 연속 스트라이크 포함.
- 지금까지의 구현은 1~9프레임에서는 정확하지만, **10번째(마지막) 프레임에서 스페어/스트라이크가 나왔을 때 필요한 추가 투구(보너스 투구)를 포함한 게임 전체의 총점 계산**은 아직 완전히 검증되지 않았을 수 있다 (특히 게임의 마지막 부분에서 "다음 프레임"이 존재하지 않는 경계 상황).

## 볼링 규칙 요약 (10번째 프레임)
- 10번째(마지막) 프레임에서 플레이어가 스페어나 스트라이크를 기록하면, 그 프레임을 완성하기 위한 추가 투구를 할 수 있다.
  - 첫 두 투구 합이 10 미만(오픈 프레임): 투구 2회로 종료, 추가 투구 없음.
  - 첫 두 투구 합이 정확히 10(스페어): 추가로 1회 더 투구 가능 (총 3투구).
  - 첫 투구가 스트라이크(10핀): 추가로 2회 더 투구 가능 (총 3투구).
- 이 추가 투구들은 오직 10번째 프레임의 보너스 계산을 위한 것이며, 그 자체로 별도의 11번째 프레임이 생기는 것은 아니다. 10번째 프레임의 점수 = 그 프레임에서 던진 모든 투구(2회 또는 3회)의 핀 수 합.
- 퍼펙트 게임: 모든 프레임이 스트라이크인 경우, 총 12번의 투구(1~9프레임 각 1투구 + 10프레임 3투구)로 총점 300점이 된다.

## 이번 phase에서 해야 할 일
`Game` 클래스의 `score()` 로직이 게임 전체(1~10프레임, 10번째 프레임의 보너스 투구 포함)에 걸쳐 정확한 총점을 계산하도록 완성한다. 특히:
- 9번째 프레임에서 스트라이크/스페어가 발생했을 때, 그 보너스로 필요한 "다음 투구들"이 10번째 프레임의 (보너스 포함) 투구들로부터 올바르게 채워지는지 확인한다.
- 10번째 프레임 자체가 스페어/스트라이크일 때, 그 보너스 투구들이 다시 다른 프레임의 보너스로 재사용되지 않고 10번째 프레임 점수 계산에만 사용되는지 확인한다 (10번째 프레임 이후에는 더 이상 보너스를 계산할 다음 프레임이 없음).
- 이전 phase들의 시나리오(오픈 프레임, 스페어, 스트라이크, 연속 스트라이크)가 여전히 정확히 동작하는지 회귀 확인.

## 입출력 예시 (이번 phase에서 통과해야 하는 시나리오)

- 시나리오 A — 퍼펙트 게임: 12번의 투구, 모두 `10` (스트라이크 12회).
  - `score()` == `300`.

- 시나리오 B — 9프레임까지는 거터, 10프레임에서 스트라이크 후 보너스 두 번: 1~9프레임 각각 `0, 0` (18번의 0 투구), 10프레임 `10, 3, 4`.
  - 계산: 1~9프레임 = 0. 10프레임 = 10 + 3 + 4 = 17.
  - `score()` == `17`.

- 시나리오 C — 9프레임까지는 거터, 10프레임에서 스페어 후 보너스 한 번: 1~9프레임 각각 `0, 0` (18번의 0 투구), 10프레임 `5, 5, 6`.
  - 계산: 10프레임 = 5 + 5 + 6 = 16.
  - `score()` == `16`.

- 시나리오 D — 9프레임까지는 거터, 10프레임은 오픈 프레임(보너스 없음): 1~9프레임 각각 `0, 0`, 10프레임 `4, 3`.
  - `score()` == `7`.

- 시나리오 E — 9번째 프레임이 스트라이크이고 10번째 프레임도 스트라이크 + 보너스: 1~8프레임 모두 `0, 0` (16번의 0), 9프레임 `10`, 10프레임 `10, 2, 3`.
  - 계산: 9프레임 = 10 + (10번째 프레임의 다음 두 투구: `10`, `2`) = 10 + 10 + 2 = 22. 10프레임 = 10 + 2 + 3 = 15.
  - `score()` == `37`.

각 시나리오는 `roll`을 순서대로 호출한 뒤 `score()`로 검증한다.
```cpp
Game game;
for (int i = 0; i < 12; ++i) {
    game.roll(10);
}
EXPECT_EQ(300, game.score());
```

## 완료 조건
- `build-and-test.ps1` 실행 시 빌드 성공, 위 시나리오(A~E)를 포함한 gmock 테스트가 모두 통과 (종료 코드 0).
- Phase 1~3에서 이미 통과하던 모든 시나리오(오픈 프레임, 스페어, 스트라이크, 연속 스트라이크)의 테스트가 계속 통과한다 (회귀 없음).
- `Game`의 public 인터페이스(`roll(int pins)`, `score()`)는 변경하지 않는다.
- 이 phase를 마치면 요구사항에 기술된 볼링 점수 계산 규칙(오픈 프레임/스페어/스트라이크/10번째 프레임)이 모두 구현된 것으로 간주하고, 전체 구현이 완료된 것으로 판단한다.
