# Phase 2 — 스페어(Spare) 보너스

## 배경
이 리포지토리는 Visual Studio C++ 프로젝트이며, gmock을 사용해 테스트를 작성한다. 빌드/테스트는 리포지토리 루트의 `build-and-test.ps1`로 수행한다 (MSBuild로 `bowling-tdd.slnx`를 빌드한 뒤 `x64\Debug\bowling-tdd.exe`를 실행, 종료 코드로 성공/실패 판정).

이전 phase(Phase 1)에서 이미 다음이 갖춰져 있다고 가정한다:
- gmock 테스트를 실행하는 진입점(`main.cpp` 등)과 `bowling-tdd.exe`가 gmock 테스트 러너로 동작하도록 프로젝트 설정이 완료됨.
- `Game` 클래스(`roll(int pins)`, `score()`)가 구현되어 있고, 스트라이크/스페어가 없는 "오픈 프레임" 게임(예: 전부 거터, 전부 1핀)에 대해서는 정확한 점수를 계산함.
- 관련 테스트 파일(예: `GameTest.cpp`)이 이미 존재하며 통과 상태임.

## 볼링 규칙 요약 (스페어)
- 스페어(Spare): 한 프레임에서 두 번의 투구로 정확히 10개의 핀을 모두 쓰러뜨린 경우.
- 스페어 프레임의 점수 = 그 프레임에서 쓰러뜨린 핀 수(항상 10) + **바로 다음 투구 1회**에서 쓰러뜨린 핀 수 (보너스).
- 이 보너스 투구는 다음 프레임의 첫 번째 투구이며, 그 투구 자체는 다음 프레임 점수 계산에도 정상적으로 포함된다 (이중으로 계산되는 것이 정상 — 보너스로 한 번 더하고, 해당 프레임 점수 계산 시에도 포함).

## 이번 phase에서 해야 할 일
기존 `Game` 클래스의 `score()` 계산 로직에 스페어 보너스 규칙을 추가로 구현한다. 이미 구현되어 있는 오픈 프레임 채점 로직은 계속 정확하게 동작해야 한다 (회귀 없이).

이번 phase 범위에서는 아래 두 가지만 고려하면 된다.
- 오픈 프레임 (두 투구 합 10 미만) — 이미 Phase 1에서 구현됨, 계속 정상 동작해야 함.
- 스페어 프레임 (두 투구 합이 정확히 10) — 이번 phase에서 새로 구현.

스트라이크(첫 투구에서 10핀)는 이번 phase 범위 밖이다 (Phase 3에서 다룸). 따라서 이번 phase의 테스트 시나리오에는 스트라이크가 포함되지 않는다.

## 입출력 예시 (이번 phase에서 통과해야 하는 시나리오)

- 시나리오 A — 한 번의 스페어 후 정상 진행: 첫 프레임에서 `5, 5` (스페어), 다음 투구 `3`, 그 이후 나머지 17번의 투구는 모두 `0`.
  - 계산: 1프레임 점수 = 10(스페어) + 3(보너스) = 13. 2프레임 점수 = 3 + 0 = 3. 나머지 프레임은 0.
  - `score()` == `16`.

- 시나리오 B — 게임 전체가 스페어의 연속: 예를 들어 10프레임 모두 `(5, 5)`로 투구하고, 10프레임(마지막 프레임)의 스페어에 대한 보너스로 마지막에 투구 1회(`3`)를 추가로 던짐 (총 21번의 투구: 9프레임×2 + 마지막 프레임 2번 + 보너스 1번).
  - 1~9프레임: 각 프레임 10(스페어) + 5(다음 프레임 첫 투구) = 15점씩 → 9 × 15 = 135.
  - 10프레임: 10(스페어) + 3(보너스 투구) = 13.
  - `score()` == `148`.

각 시나리오는 `roll`을 순서대로 호출한 뒤 `score()`로 검증한다.
```cpp
Game game;
game.roll(5);
game.roll(5); // spare
game.roll(3);
for (int i = 0; i < 17; ++i) {
    game.roll(0);
}
EXPECT_EQ(16, game.score());
```

## 완료 조건
- `build-and-test.ps1` 실행 시 빌드 성공, 위 시나리오들을 포함한 gmock 테스트가 모두 통과 (종료 코드 0).
- Phase 1에서 이미 통과하던 오픈 프레임 시나리오(거터 게임, 전부 1핀 등)의 테스트가 계속 통과한다 (회귀 없음).
- `Game`의 public 인터페이스(`roll(int pins)`, `score()`)는 변경하지 않는다.
