# Phase 3 — 스트라이크(Strike) 보너스

## 배경
이 리포지토리는 Visual Studio C++ 프로젝트이며, gmock을 사용해 테스트를 작성한다. 빌드/테스트는 리포지토리 루트의 `build-and-test.ps1`로 수행한다 (MSBuild로 `bowling-tdd.slnx`를 빌드한 뒤 `x64\Debug\bowling-tdd.exe`를 실행, 종료 코드로 성공/실패 판정).

이전 phase들에서 이미 다음이 갖춰져 있다고 가정한다:
- gmock 테스트 실행 진입점과 `bowling-tdd.exe`가 gmock 테스트 러너로 동작하도록 프로젝트 설정이 완료됨.
- `Game` 클래스(`roll(int pins)`, `score()`)가 구현되어 있고, 다음이 이미 정확히 계산됨:
  - 오픈 프레임 게임 (두 투구 합이 10 미만인 프레임들로만 구성).
  - 스페어가 포함된 게임 (두 투구 합이 정확히 10인 프레임의 보너스 = 다음 투구 1회).
- 관련 테스트 파일(예: `GameTest.cpp`)이 이미 존재하며 통과 상태임.

## 볼링 규칙 요약 (스트라이크)
- 스트라이크(Strike): 한 프레임의 **첫 번째** 투구에서 10개의 핀을 모두 쓰러뜨린 경우. 이 경우 그 프레임은 투구를 한 번만 하고 종료된다 (두 번째 투구가 없음).
- 스트라이크 프레임의 점수 = 10 + **바로 다음 두 번의 투구**에서 쓰러뜨린 핀 수의 합 (보너스).
- "다음 두 번의 투구"는 다음 프레임의 투구들을 의미한다. 만약 다음 프레임도 스트라이크라면, 다음 두 번의 투구는 "다음 프레임의 투구 1회(스트라이크, 10핀)" + "그 다음 프레임의 첫 번째 투구"가 된다 (연속 스트라이크 처리).

## 이번 phase에서 해야 할 일
기존 `Game` 클래스의 `score()` 계산 로직에 스트라이크 보너스 규칙을 추가로 구현한다. 이미 구현되어 있는 오픈 프레임/스페어 채점 로직은 계속 정확하게 동작해야 한다 (회귀 없이).

이번 phase 범위에서 고려해야 하는 것:
- 오픈 프레임 — 이미 구현됨, 계속 정상 동작해야 함.
- 스페어 프레임 — 이미 구현됨, 계속 정상 동작해야 함.
- 스트라이크 프레임 (첫 투구 10핀) — 이번 phase에서 새로 구현. 단일 스트라이크와, 연속된 스트라이크(더블/트리플) 모두 처리한다.

10번째(마지막) 프레임에서 스트라이크가 나왔을 때의 추가 투구(보너스 투구) 처리는 이번 phase의 필수 범위가 아니다 (Phase 4에서 다룸). 이번 phase의 테스트 시나리오는 스트라이크가 게임 중간(1~9프레임)에서만 발생하도록 구성하여, 보너스에 필요한 "다음 두 번의 투구"가 항상 정상적인 프레임 투구로 확보되도록 한다.

## 입출력 예시 (이번 phase에서 통과해야 하는 시나리오)

- 시나리오 A — 하나의 스트라이크만 있는 게임: 1프레임에서 스트라이크(`10`), 2프레임에서 `3, 4`, 나머지 프레임은 모두 `0`.
  - 투구 순서: `10, 3, 4` 그리고 이후 나머지 투구는 모두 `0` (총 투구 횟수는 프레임 규칙에 맞게 자연스럽게 구성, 이 kata에서는 투구 횟수/프레임 수 검증을 하지 않으므로 나머지를 16번의 `0` 투구로 채워도 무방).
  - 계산: 1프레임 = 10 + 3 + 4 = 17. 2프레임 = 3 + 4 = 7. 나머지 0.
  - `score()` == `24`.

- 시나리오 B — 연속 두 번의 스트라이크(더블): 1프레임 스트라이크(`10`), 2프레임 스트라이크(`10`), 3프레임 `3, 4`, 나머지 프레임 모두 `0`.
  - 계산: 1프레임 = 10 + 10 + 3 = 23. 2프레임 = 10 + 3 + 4 = 17. 3프레임 = 3 + 4 = 7.
  - `score()` == `47`.

- 시나리오 C — 유명한 예시: 첫 9프레임 모두 스트라이크, 10프레임은 `9, 0` (스트라이크 아님, 오픈 프레임).
  - 이 예시로 연속 스트라이크(트리플 이상) 처리가 정확한지 검증한다. 직접 합산하여 기대값을 계산해 테스트에 반영한다.

각 시나리오는 `roll`을 순서대로 호출한 뒤 `score()`로 검증한다.
```cpp
Game game;
game.roll(10); // strike
game.roll(3);
game.roll(4);
for (int i = 0; i < 16; ++i) {
    game.roll(0);
}
EXPECT_EQ(24, game.score());
```

## 완료 조건
- `build-and-test.ps1` 실행 시 빌드 성공, 위 시나리오들을 포함한 gmock 테스트가 모두 통과 (종료 코드 0).
- Phase 1~2에서 이미 통과하던 시나리오(오픈 프레임, 스페어)의 테스트가 계속 통과한다 (회귀 없음).
- `Game`의 public 인터페이스(`roll(int pins)`, `score()`)는 변경하지 않는다.
