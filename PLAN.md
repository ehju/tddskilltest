# PLAN: 볼링 점수 계산 (The Bowling Game Kata)

## 목표
`bowling-tdd` Visual Studio C++ 프로젝트에 gmock 기반 TDD로 `Game` 클래스를 구현한다.
`Game` 클래스는 다음 두 메서드를 제공해야 한다.

- `void roll(int pins)` : 한 번의 투구를 기록한다. `pins`는 이번 투구로 쓰러뜨린 핀 수(0~10)이다.
- `int score()` : 지금까지 기록된 투구들을 바탕으로 게임의 총점을 계산해 반환한다.

빌드/테스트는 리포지토리 루트의 `build-and-test.ps1` 스크립트로 수행한다 (MSBuild로 `bowling-tdd.slnx`를 빌드한 뒤 생성된 `bowling-tdd.exe`를 실행하여 gmock 테스트를 구동함). 이 스크립트가 0을 반환해야 해당 phase가 통과한 것이다.

## 현재 상태 (계획 수립 시점)
`bowling-tdd/` 프로젝트에는 아직 `.cpp`/`.h` 소스 파일이 하나도 없다 (packages.config만 참조된 빈 프로젝트). 따라서 Phase 1에서 gmock 테스트 실행 기반(엔트리 포인트 포함)과 `Game` 클래스의 뼈대를 함께 만들어야 한다.

## Phase 구성

### Phase 1 — 프로젝트 부트스트랩 + 기본 프레임 점수 (거터 게임 / 오픈 프레임)
- gmock 테스트를 실행할 수 있는 최소한의 빌드 골격을 구성한다 (`main.cpp`에서 `InitGoogleMock` + `RUN_ALL_TESTS`, 신규 소스 파일들을 `bowling-tdd.vcxproj`/`.filters`에 등록).
- `Game` 클래스(`roll`, `score`)를 구현하되, 스트라이크/스페어 보너스 없이 "모든 프레임이 오픈 프레임(스트라이크/스페어가 아님)"인 경우만 올바르게 계산하면 된다.
- 대표 케이스: 전부 거터(모든 투구 0핀) → 총점 0. 전부 1핀씩 20번 투구 → 총점 20.

### Phase 2 — 스페어(Spare) 보너스
- 한 프레임에서 두 번의 투구 합이 정확히 10인 경우(스페어)의 보너스 규칙을 구현한다: 스페어 프레임 점수 = 10 + 바로 다음 투구 1회의 핀 수.
- 나머지 프레임은 오픈 프레임 규칙(Phase 1)을 그대로 따른다.

### Phase 3 — 스트라이크(Strike) 보너스
- 첫 투구에서 10핀을 모두 쓰러뜨린 경우(스트라이크)의 보너스 규칙을 구현한다: 스트라이크 프레임 점수 = 10 + 바로 다음 두 번의 투구 핀 수 합. 스트라이크 프레임은 투구를 1회만 소비한다.
- 스트라이크 다음에 다시 스트라이크가 이어지는 경우(연속 스트라이크)도 올바르게 계산되어야 한다.

### Phase 4 — 10번째 프레임 및 퍼펙트 게임(완전한 게임)
- 10번째 프레임에서 스페어/스트라이크가 나왔을 때 허용되는 추가 투구(최대 3투구까지)를 포함해 전체 게임 점수를 정확히 계산한다.
- 퍼펙트 게임(모든 프레임 스트라이크, 12번 투구, 총점 300)을 포함한 경계 케이스를 검증한다.
- 스트라이크/스페어/오픈 프레임이 섞인 일반적인 게임 시퀀스도 정확히 계산되어야 한다.

## 진행 방식
각 phase는 `phase{N}.md`에 기술된 요구사항만을 대상으로 RED → GREEN → REFACTOR 사이클을 독립적으로 수행한다. 각 phase 담당 에이전트는 이전 phase에서 어떤 논의가 있었는지 알지 못하므로, 각 phase 문서는 필요한 배경(선행 phase에서 이미 구현되어 있다고 가정할 수 있는 내용 포함)을 자체적으로 명시한다.
